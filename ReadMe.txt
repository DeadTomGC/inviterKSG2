Overview:

MySQLClient.cs is not used. I did find it to be a useful reference.

Ok, so it might be working now.  Checker checks the database, and the main does it's thing of maintaining the connection.

Welp, it's pretty simple. We'll see if it works. 